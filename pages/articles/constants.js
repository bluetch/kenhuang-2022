import React from "react";

export const CAMINO_IMG_ROOT_URL = "/images/articles/camino";

// Provide a minimal page component so Next treats this file as a valid page.
// If this file was intended only as a module of constants, consider moving
// it to `constants/` instead and removing the page file.
export default function ConstantsPage() {
	return null;
}